---
'focus-trap-react': major
---

Dropping `propTypes` no longer supported by React 19 (going forward, use TypeScript for prop typings, and if necessary, a runtime library to validate props); Increasing minimum supported React version up to >=18
